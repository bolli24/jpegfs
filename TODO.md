- store directory parent on inode so that we can save directory entries pages for empty directories
  (or store a parent-child hashmap and infer relationship on load)
- encryption
- lsb shuffling
- better encoding: matrix instead of lsb